# Copy to .env on the server and choose a password.
# Leave DASHBOARD_PASSWORD empty only if the port is NOT exposed to the internet.
DASHBOARD_USER=btcsim
DASHBOARD_PASSWORD=escolhe-uma-password
# Optional: stable Flask session key (defaults to a hash of the dashboard password).
# FLASK_SECRET_KEY=
# Push de hora a hora. No telemóvel, abre https://ntfy.sh/ESTE_TOPICO e permite notificações.
NTFY_TOPIC=
NTFY_URL=https://ntfy.sh
DASHBOARD_URL=http://91.99.167.243:8000
